/* Canonical Pablicus sharing. Custom-domain cutover changes one meta value only. */
(function(root){'use strict';const FALLBACK='https://matveyryabokon30-crypto.github.io/vision-talk/pablicus/';function canonical(){const v=document.querySelector('meta[name="pablicus-canonical"]')?.content;return new URL(v||FALLBACK,location.href).href;}async function share({title='Pablicus',text='Присоединяйся ко мне в Pablicus',url=canonical()}={}){if(navigator.share){try{await navigator.share({title,text,url});return true}catch(e){if(e.name==='AbortError')return false;}}try{await navigator.clipboard.writeText(url);return true}catch{return false;}}function mount(){for(const host of document.querySelectorAll('.youSettings,.settingsPage,.youForm'))if(!host.querySelector('.pablicusInviteButton,#shareApp')){const b=document.createElement('button');b.type='button';b.className='setting pablicusInviteButton';b.textContent='Пригласить в Pablicus';b.onclick=()=>share();host.append(b);}}let queued=false;new MutationObserver(()=>{if(!queued){queued=true;requestAnimationFrame(()=>{queued=false;mount();});}}).observe(document.body,{subtree:true,childList:true});mount();root.PablicusInvite=Object.freeze({share,canonical});})(window);