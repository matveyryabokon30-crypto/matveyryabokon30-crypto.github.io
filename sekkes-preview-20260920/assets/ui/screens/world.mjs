import {el,picture,icon} from '../components.mjs';
import {worldAdapter} from '../product-adapters.mjs';
export function create(ctx){const n=el('section','screen world-screen');n.setAttribute('aria-label','Живой мир');n.append(picture('world','Концепт островного мира: дома, вода, лес и персонажи. Иллюстрация, не симуляция.','world-art'));const head=el('div','world-heading');head.append(el('h1','','Живой мир'),el('span','badge','Визуальный preview'));const panel=el('div','world-panel');panel.append(el('p','',worldAdapter.description));const b=el('button','secondary-action','О мире');b.onclick=()=>ctx.info('Мир, который живёт сам','Среда, строительство и автономные персонажи — отдельный игровой продукт. Сервер определяет события мира, движок проверяет действия, у каждого персонажа своя память и ограниченное восприятие. Личные разговоры не передаются персонажам. Вход, строительство и заселение появятся после подключения и проверки движка.');panel.append(b);n.append(head,panel);return {node:n,dispose(){}}}
