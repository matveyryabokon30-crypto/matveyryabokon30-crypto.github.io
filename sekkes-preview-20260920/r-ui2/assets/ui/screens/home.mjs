import {el,picture} from '../components.mjs';
export function create(ctx){const n=el('section','screen home-screen');n.setAttribute('aria-label','Главная');const scene=el('div','wallpaper');function update(){scene.replaceChildren(picture(document.documentElement.dataset.theme==='light'?'lake-day':'lake-night','','scene-image'))}update();const observer=new MutationObserver(update);observer.observe(document.documentElement,{attributes:true,attributeFilter:['data-theme']});const start=el('button','scene-start');start.setAttribute('aria-label','Начать разговор');start.onclick=()=>ctx.runtime()?.toggleMic();n.append(scene,start);return {node:n,dispose(){observer.disconnect();start.onclick=null}}}
