import {ImageMagick,initializeImageMagick,MagickFormat} from 'npm:@imagemagick/magick-wasm@0.0.30';
let ready:Promise<void>|null=null;let serial:Promise<unknown>=Promise.resolve();
function init(){return ready ||= (async()=>{const b=await Deno.readFile(new URL('magick.wasm',import.meta.resolve('npm:@imagemagick/magick-wasm@0.0.30')));await initializeImageMagick(b);})();}
export function derivatives(bytes:Uint8Array,mime:string):Promise<{size:number,bytes:Uint8Array,width:number,height:number}[]>{const work=serial.catch(()=>{}).then(async()=>{await init();return ImageMagick.read(bytes,image=>{if(image.width*image.height>20_000_000)throw Error('image_dimensions_limit');image.autoOrient();const result=[];for(const size of [1600,960,192]){if(Math.max(image.width,image.height)>size)image.thumbnail(size,size);image.strip();image.quality=size===1600?82:80;image.settings.setDefine(MagickFormat.WebP,'method','1');const output=image.write(MagickFormat.WebP,data=>new Uint8Array(data));if(output.byteLength>4*1024*1024)throw Error('preview_size_limit');result.push({size,bytes:output,width:image.width,height:image.height});}return result;});});serial=work;return work;}
